{
  "name": "penktadieni-testas-1-fix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "dead-server --port=3000 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "fix, relative/absolute"
  ],
  "author": "Povilas",
  "license": "ISC",
  "devDependencies": {
    "dead-server": "^1.0.6"
  }
}
